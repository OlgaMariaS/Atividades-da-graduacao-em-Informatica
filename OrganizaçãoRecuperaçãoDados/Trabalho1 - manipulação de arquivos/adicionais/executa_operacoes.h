#include <stdio.h>
#include <string.h>
#include <stdlib.h>


/*Função que le o "arquivo_de_operacoes.txt", nela será lido o primeiro digito que 
definirá a operação a ser realizada sendo i - inserir, r-remover, b-buscar, os caracteres após o primeiro 
serão armazenados como chaves (dados a serem trabalhados) */

/*//nome do arquivo será passado por argv[2]
função executa operações (recebe o nome do arquivo) {
char operacao[2];
char chave[50];
cria o FILE * arquivo _de_operacoes
abre o arquivo para leitura
le o primeiro caracter e atribui em operacao 
enquanto (operacao ter caracter continua, se não tiver nada (EOF)  para) {
se operacao == r { chamar a função remover, e passa a chave como parametro }
se operacao == b { chamar a função busca, e passa a chave como parametro }
se operacao == i { chamar a função inserir, e passa a chave como parametro }
}
fim das operacoes que estavam arquivo de operacoes
}
*/

void executa_operacoes(char nome_do_arquivo_de_operacoes[30]){

}